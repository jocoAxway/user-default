class Endpoints {
	static get schema() {
		// not able to reference this schema for some reason - got validation error:
		// "A deterministic version of a JSON Schema object."
		// '$ref': 'https://raw.githubusercontent.com/OAI/OpenAPI-Specification/master/schemas/v2.0/schema.json'
		return {
			type: 'object',
			description: 'One or more Swagger (OpenAPI) specification documents'
		};
	}
}

exports = module.exports = Endpoints;
