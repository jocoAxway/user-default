const { FlowNode } = require('axway-flow');

class ArrowNode extends FlowNode {
	constructor(apibuilder, name, node, spec, flow) {
		super(name, node, spec, flow);
		this.apibuilder = apibuilder;
	}
}

exports = module.exports = ArrowNode;
