const validator = /^-?\d*\.?\d+$/;

/**
 * A number type.
 * @param obj
 * @returns {object}
 */
const NumField = obj => ({
	type: 'text',
	subtype: 'number',
	required: false,
	default: 0,
	validator,
	...obj
});

module.exports = NumField;
